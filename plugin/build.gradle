apply plugin: 'maven'

dependencies {
    implementation 'com.github.tomokinakamaru:manifest-attributes:ad0c2fef88'
    implementation gradleApi()
}

jar {
    manifest {
        attributes('Antlr-Version': antlrVersion)
    }
}

repositories {
    maven { url 'https://jitpack.io' }
}
